type Query {
    searchListings(prompt: String!): SearchResult!
}

type SearchResult {
    filters: SearchFilters!
    listings: [Listing!]!
    total: Int!
}

type Listing {
    id: ID!
    address: ListingAddress!
    price: Float!
    beds: Int
    baths: Float
    sqft: Int
    propertyType: String
    imageUrl: String
    listingUrl: String
    description: String
}

type ListingAddress {
    street: String
    city: String
    state: String
    zip: String
}

type SearchFilters {
    location: String
    minBeds: Int
    maxBeds: Int
    minBaths: Int
    maxPrice: Float
    minPrice: Float
    propertyType: String
    features: [String!]!
}
